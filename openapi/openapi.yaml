openapi: 3.0.3
info:
  title: go-proxy-mini Admin API
  version: 1.0.0
  description: '管理端 API（模板/账号/分组/规则/日志/统计）。认证：Authorization: Bearer <admin_token>。'
servers:
  - url: /admin
tags:
  - name: templates
  - name: accounts
  - name: groups
  - name: rules
  - name: logs
  - name: stats
paths:
  /templates:
    post:
      operationId: PostTemplates
      tags: [templates]
      summary: 创建模板
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/TemplateCreate' }
      responses:
        '200':
          description: 创建后的模板
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Template' }
        default:
          $ref: '#/components/responses/Error'
    get:
      operationId: GetTemplates
      tags: [templates]
      summary: 模板列表（分页/筛选/排序）
      parameters:
        - { name: limit, in: query, schema: { type: integer, default: 20 } }
        - { name: offset, in: query, schema: { type: integer, default: 0 } }
        - { name: name, in: query, schema: { type: string } }
        - { name: sort, in: query, schema: { type: string } }
        - { name: order, in: query, schema: { type: string, enum: [asc, desc] } }
      responses:
        '200':
          description: 模板列表
          content:
            application/json:
              schema: { $ref: '#/components/schemas/TemplateListResponse' }
        default:
          $ref: '#/components/responses/Error'
  /templates/batch-delete:
    post:
      operationId: PostTemplatesBatchDelete
      tags: [templates]
      summary: 批量删除模板（事务，全成或全败）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/BatchDeleteBody' }
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/BatchDeleteResponse' }
        default:
          $ref: '#/components/responses/Error'
  /templates/batch-update:
    post:
      operationId: PostTemplatesBatchUpdate
      tags: [templates]
      summary: 批量更新模板（fields 为任意字段子集）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/BatchUpdateTemplatesBody' }
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/BatchUpdateResponse' }
        default:
          $ref: '#/components/responses/Error'
  /templates/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
    get:
      operationId: GetTemplatesId
      tags: [templates]
      responses:
        '200':
          description: 模板
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Template' }
        default:
          $ref: '#/components/responses/Error'
    put:
      operationId: PutTemplatesId
      tags: [templates]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/TemplateCreate' }
      responses:
        '200':
          description: 更新后模板
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Template' }
        default:
          $ref: '#/components/responses/Error'
    delete:
      operationId: DeleteTemplatesId
      tags: [templates]
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DeletedResponse' }
        default:
          $ref: '#/components/responses/Error'
  /accounts:
    post:
      operationId: PostAccounts
      tags: [accounts]
      summary: 创建账号
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/AccountCreate' }
      responses:
        '200':
          description: 创建后的账号
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Account' }
        default:
          $ref: '#/components/responses/Error'
    get:
      operationId: GetAccounts
      tags: [accounts]
      summary: 账号列表（分页/筛选/排序，含运行时视图）
      parameters:
        - { name: limit, in: query, schema: { type: integer, default: 20 } }
        - { name: offset, in: query, schema: { type: integer, default: 0 } }
        - { name: name, in: query, schema: { type: string } }
        - { name: sort, in: query, schema: { type: string } }
        - { name: order, in: query, schema: { type: string, enum: [asc, desc] } }
        - { name: status, in: query, schema: { type: string, description: 多值逗号分隔：active,unhealthy,429,disabled } }
        - { name: template_id, in: query, schema: { type: integer, format: int64 } }
      responses:
        '200':
          description: 账号视图列表
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AccountListResponse' }
        default:
          $ref: '#/components/responses/Error'
  /accounts/batch-delete:
    post:
      operationId: PostAccountsBatchDelete
      tags: [accounts]
      summary: 批量删除账号（事务，全成或全败）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/BatchDeleteBody' }
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/BatchDeleteResponse' }
        default:
          $ref: '#/components/responses/Error'
  /accounts/batch-update:
    post:
      operationId: PostAccountsBatchUpdate
      tags: [accounts]
      summary: 批量更新账号（fields 为任意字段子集）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/BatchUpdateAccountsBody' }
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/BatchUpdateResponse' }
        default:
          $ref: '#/components/responses/Error'
  /accounts/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
    get:
      operationId: GetAccountsId
      tags: [accounts]
      responses:
        '200':
          description: 账号
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Account' }
        default:
          $ref: '#/components/responses/Error'
    put:
      operationId: PutAccountsId
      tags: [accounts]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/AccountCreate' }
      responses:
        '200':
          description: 更新后账号
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Account' }
        default:
          $ref: '#/components/responses/Error'
    delete:
      operationId: DeleteAccountsId
      tags: [accounts]
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DeletedResponse' }
        default:
          $ref: '#/components/responses/Error'
  /accounts/{id}/groups:
    parameters:
      - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
    get:
      operationId: GetAccountsIdGroups
      tags: [accounts]
      summary: 读取账号的全部分组 id（编辑回显；不随账号列表返回）
      responses:
        '200':
          description: 分组 id 列表
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AccountGroupsResponse' }
        default:
          $ref: '#/components/responses/Error'
  /groups:
    post:
      operationId: PostGroups
      tags: [groups]
      summary: 创建分组（平台容量池；key 为独立表，由用户面 /user/keys 创建）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/GroupCreate' }
      responses:
        '200':
          description: 创建后的分组
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Group' }
        default:
          $ref: '#/components/responses/Error'
    get:
      operationId: GetGroups
      tags: [groups]
      summary: 分组列表（分页/筛选/排序）
      parameters:
        - { name: limit, in: query, schema: { type: integer, default: 20 } }
        - { name: offset, in: query, schema: { type: integer, default: 0 } }
        - { name: name, in: query, schema: { type: string } }
        - { name: sort, in: query, schema: { type: string } }
        - { name: order, in: query, schema: { type: string, enum: [asc, desc] } }
      responses:
        '200':
          description: 分组列表
          content:
            application/json:
              schema: { $ref: '#/components/schemas/GroupListResponse' }
        default:
          $ref: '#/components/responses/Error'
  /groups/batch-delete:
    post:
      operationId: PostGroupsBatchDelete
      tags: [groups]
      summary: 批量删除分组（事务，全成或全败）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/BatchDeleteBody' }
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/BatchDeleteResponse' }
        default:
          $ref: '#/components/responses/Error'
  /groups/batch-update:
    post:
      operationId: PostGroupsBatchUpdate
      tags: [groups]
      summary: 批量更新分组（fields 为任意字段子集）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/BatchUpdateGroupsBody' }
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/BatchUpdateResponse' }
        default:
          $ref: '#/components/responses/Error'
  /groups/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
    get:
      operationId: GetGroupsId
      tags: [groups]
      responses:
        '200':
          description: 分组
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Group' }
        default:
          $ref: '#/components/responses/Error'
    put:
      operationId: PutGroupsId
      tags: [groups]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/GroupCreate' }
      responses:
        '200':
          description: 更新后分组
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Group' }
        default:
          $ref: '#/components/responses/Error'
    delete:
      operationId: DeleteGroupsId
      tags: [groups]
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DeletedResponse' }
        default:
          $ref: '#/components/responses/Error'
  /settings:
    get:
      operationId: GetAdminSettings
      tags: [settings]
      summary: 全部设置（默认值 + DB 覆盖）
      responses:
        '200':
          description: 设置列表
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Setting' }
        default:
          $ref: '#/components/responses/Error'
    put:
      operationId: PutAdminSettings
      tags: [settings]
      summary: 更新设置（类型化校验：switch 必须 true/false、number 必须数字）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SettingUpdate' }
      responses:
        '200':
          description: 更新后的设置列表
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Setting' }
        default:
          $ref: '#/components/responses/Error'
  # --- /user 面（tag: user；生成到 internal/handler/user 独立 ServerInterface） ---
  /user/auth/register:
    post:
      operationId: PostUserAuthRegister
      tags: [user]
      summary: 注册（signup_enabled 开关检查；注册即登录返回 JWT）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/UserAuthRegister' }
      responses:
        '200':
          description: JWT + 用户
          content:
            application/json:
              schema: { $ref: '#/components/schemas/UserAuthResponse' }
        default:
          $ref: '#/components/responses/Error'
  /user/auth/login:
    post:
      operationId: PostUserAuthLogin
      tags: [user]
      summary: 登录（bcrypt 校验 → JWT）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/UserAuthLogin' }
      responses:
        '200':
          description: JWT + 用户
          content:
            application/json:
              schema: { $ref: '#/components/schemas/UserAuthResponse' }
        default:
          $ref: '#/components/responses/Error'
  /user/auth/me:
    get:
      operationId: GetUserAuthMe
      tags: [user]
      summary: 当前用户信息（JWT）
      responses:
        '200':
          description: 用户
          content:
            application/json:
              schema: { $ref: '#/components/schemas/User' }
        default:
          $ref: '#/components/responses/Error'
  # 注意 chi 路径顺序：/rules 必须先于 /rules/{id} 注册（生成器按 spec 顺序注册）。
  /rules:
    get:
      operationId: ListRules
      tags: [rules]
      summary: 规则列表（enabled 过滤，priority 升序）
      parameters:
        - { name: enabled, in: query, schema: { type: boolean } }
      responses:
        '200':
          description: 规则列表
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RuleListResponse' }
        default:
          $ref: '#/components/responses/Error'
    post:
      operationId: CreateRule
      tags: [rules]
      summary: 创建规则
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RuleCreate' }
      responses:
        '201':
          description: 创建后的规则
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Rule' }
        default:
          $ref: '#/components/responses/Error'
  /rules/batch-delete:
    post:
      operationId: PostRulesBatchDelete
      tags: [rules]
      summary: 批量删除规则（事务，全成或全败）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/BatchDeleteBody' }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/BatchDeleteResponse' }
        default:
          $ref: '#/components/responses/Error'
  /rules/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
    put:
      operationId: UpdateRule
      tags: [rules]
      summary: 更新规则（fields 任意子集，未提供字段保持原值）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RulePatch' }
      responses:
        '200':
          description: 更新后的规则
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Rule' }
        default:
          $ref: '#/components/responses/Error'
    delete:
      operationId: DeleteRule
      tags: [rules]
      summary: 删除规则
      responses:
        '204':
          description: 删除成功
        default:
          $ref: '#/components/responses/Error'
  /logs:
    get:
      operationId: GetLogs
      tags: [logs]
      summary: 用量日志分页查询
      parameters:
        - { name: limit, in: query, schema: { type: integer, default: 20 } }
        - { name: offset, in: query, schema: { type: integer, default: 0 } }
        - { name: group_id, in: query, schema: { type: integer, format: int64 } }
        - { name: account_id, in: query, schema: { type: integer, format: int64 } }
        - { name: model, in: query, schema: { type: string } }
        - { name: status_code, in: query, schema: { type: integer } }
        - { name: error_type, in: query, schema: { type: string } }
        - { name: from, in: query, schema: { type: string, format: date-time } }
        - { name: to, in: query, schema: { type: string, format: date-time } }
      responses:
        '200':
          description: 分页结果
          content:
            application/json:
              schema: { $ref: '#/components/schemas/LogsResponse' }
        default:
          $ref: '#/components/responses/Error'
  /stats:
    get:
      operationId: GetStats
      tags: [stats]
      summary: 用量统计聚合
      parameters:
        - { name: from, in: query, schema: { type: string, format: date-time } }
        - { name: to, in: query, schema: { type: string, format: date-time } }
        - { name: granularity, in: query, schema: { type: string, enum: [hour, day], default: day } }
        - { name: group_id, in: query, schema: { type: integer, format: int64 } }
        - { name: account_id, in: query, schema: { type: integer, format: int64 } }
        - { name: model, in: query, schema: { type: string } }
      responses:
        '200':
          description: 统计桶数组
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/StatBucket' }
        default:
          $ref: '#/components/responses/Error'
components:
  responses:
    Error:
      description: 错误响应
      content:
        application/json:
          schema: { $ref: '#/components/schemas/ErrorResponse' }
  schemas:
    ErrorResponse:
      type: object
      required: [error]
      properties:
        error: { type: string }
    DeletedResponse:
      type: object
      required: [deleted]
      properties:
        deleted: { type: boolean }
    RequestFormat:
      type: string
      enum: [openai-chat, openai-responses, anthropic]
    AccountStatus:
      type: string
      enum: [active, unhealthy, "429", disabled]
    ErrorType:
      type: string
      enum: [none, "429", "4xx", "5xx", network, auth, no_account, abort]
    TemplateCreate:
      type: object
      required: [name, base_url, supported_formats]
      properties:
        name: { type: string }
        base_url: { type: string }
        # credential_type 模板级：一个模板 = 一种号池；缺省 = api_key。
        # 号池生态类型后续追加枚举（codex_oauth 等）。
        credential_type: { type: string, enum: [api_key], default: api_key }
        supported_formats:
          type: array
          minItems: 1
          items: { type: string, enum: [openai-chat, openai-responses, anthropic] }
        models:
          type: array
          items: { type: string }
        format_models:
          type: object
          additionalProperties:
            type: array
            items: { type: string }
        model_mapping:
          type: object
          additionalProperties: { type: string }
    Template:
      type: object
      required: [ID, Name, BaseURL, SupportedFormats, CreatedAt, UpdatedAt]
      properties:
        ID: { type: integer, format: int64 }
        Name: { type: string }
        BaseURL: { type: string }
        CredentialType: { type: string }
        SupportedFormats:
          type: array
          items: { type: string, enum: [openai-chat, openai-responses, anthropic] }
        Models:
          type: array
          items: { type: string }
        FormatModels:
          type: object
          additionalProperties:
            type: array
            items: { type: string }
        ModelMapping:
          type: object
          additionalProperties: { type: string }
        CreatedAt: { type: string, format: date-time }
        UpdatedAt: { type: string, format: date-time }
    AccountCreate:
      type: object
      required: [name, template_id, upstream_key]
      properties:
        name: { type: string }
        template_id: { type: integer, format: int64 }
        upstream_key: { type: string }
        status: { $ref: '#/components/schemas/AccountStatus' }
        weight: { type: integer }
        max_concurrency: { type: integer }
        # 创建时可选：null/缺省 = 无分组；提供 = 替换语义（空数组 = 无分组）
        group_ids:
          type: array
          items: { type: integer, format: int64 }
    Account:
      type: object
      properties:
        ID: { type: integer, format: int64 }
        Name: { type: string }
        TemplateID: { type: integer, format: int64 }
        Template: { $ref: '#/components/schemas/Template' }
        UpstreamKey: { type: string }
        Status: { $ref: '#/components/schemas/AccountStatus' }
        CooldownUntil: { type: string, format: date-time, nullable: true }
        Weight: { type: integer }
        MaxConcurrency: { type: integer }
        LastError: { type: string, nullable: true }
        LastUsedAt: { type: string, format: date-time, nullable: true }
        CreatedAt: { type: string, format: date-time }
        UpdatedAt: { type: string, format: date-time }
    AccountView:
      allOf:
        - $ref: '#/components/schemas/Account'
        - type: object
          properties:
            concurrency: { type: integer, format: int64 }
            err_rate: { type: number, format: double }
            err_count: { type: integer }
    TemplateListResponse:
      type: object
      required: [total, rows]
      properties:
        total: { type: integer, format: int64 }
        rows:
          type: array
          items: { $ref: '#/components/schemas/Template' }
    AccountListResponse:
      type: object
      required: [total, rows]
      properties:
        total: { type: integer, format: int64 }
        rows:
          type: array
          items: { $ref: '#/components/schemas/AccountView' }
    GroupListResponse:
      type: object
      required: [total, rows]
      properties:
        total: { type: integer, format: int64 }
        rows:
          type: array
          items: { $ref: '#/components/schemas/Group' }
    GroupVisibility:
      type: string
      enum: [public, private]
    UserRole:
      type: string
      enum: [platform_admin, user]
    UserStatus:
      type: string
      enum: [active, disabled]
    User:
      type: object
      properties:
        ID: { type: integer, format: int64 }
        Email: { type: string }
        Role: { $ref: '#/components/schemas/UserRole' }
        Status: { $ref: '#/components/schemas/UserStatus' }
        MaxConcurrency: { type: integer }
        Balance: { type: integer, format: int64 }
        CreatedAt: { type: string, format: date-time }
        UpdatedAt: { type: string, format: date-time }
    UserAuthRegister:
      type: object
      required: [email, password]
      properties:
        email: { type: string }
        password: { type: string }
    UserAuthLogin:
      type: object
      required: [email, password]
      properties:
        email: { type: string }
        password: { type: string }
    UserAuthResponse:
      type: object
      required: [token, user]
      properties:
        token: { type: string }
        user: { $ref: '#/components/schemas/User' }
    Setting:
      type: object
      properties:
        Key: { type: string }
        Type: { type: string, enum: [switch, number, string] }
        Value: { type: string }
        UpdatedAt: { type: string, format: date-time }
    SettingUpdate:
      type: object
      required: [key, value]
      properties:
        key: { type: string }
        value: { type: string }
    GroupCreate:
      type: object
      required: [name]
      properties:
        name: { type: string }
        visibility: { $ref: '#/components/schemas/GroupVisibility' }
    RuleCreate:
      type: object
      required: [name, priority]
      properties:
        name: { type: string }
        enabled: { type: boolean }
        priority: { type: integer }
        when:
          type: object
          additionalProperties: true
        then:
          type: object
          additionalProperties: true
    RulePatch:
      type: object
      properties:
        name: { type: string }
        enabled: { type: boolean }
        priority: { type: integer }
        when:
          type: object
          additionalProperties: true
        then:
          type: object
          additionalProperties: true
    Rule:
      type: object
      required: [ID, Name, Enabled, Priority, When, Then, CreatedAt, UpdatedAt]
      properties:
        ID: { type: integer, format: int64 }
        Name: { type: string }
        Enabled: { type: boolean }
        Priority: { type: integer }
        When:
          type: object
          additionalProperties: true
        Then:
          type: object
          additionalProperties: true
        CreatedAt: { type: string, format: date-time }
        UpdatedAt: { type: string, format: date-time }
    RuleListResponse:
      type: object
      required: [total, rows]
      properties:
        total: { type: integer, format: int64 }
        rows:
          type: array
          items: { $ref: '#/components/schemas/Rule' }
    Group:
      type: object
      properties:
        ID: { type: integer, format: int64 }
        Name: { type: string }
        Visibility: { $ref: '#/components/schemas/GroupVisibility' }
        CreatedAt: { type: string, format: date-time }
        UpdatedAt: { type: string, format: date-time }
    AccountGroupsResponse:
      type: object
      required: [group_ids]
      properties:
        group_ids:
          type: array
          items: { type: integer, format: int64 }
    BatchDeleteBody:
      type: object
      required: [ids]
      properties:
        ids:
          type: array
          items: { type: integer, format: int64 }
          minItems: 1
          maxItems: 100
    BatchUpdateTemplatesBody:
      type: object
      required: [ids, fields]
      properties:
        ids:
          type: array
          items: { type: integer, format: int64 }
          minItems: 1
          maxItems: 100
        fields:
          $ref: '#/components/schemas/TemplatePatch'
    TemplatePatch:
      type: object
      properties:
        name: { type: string }
        base_url: { type: string }
        supported_formats:
          type: array
          minItems: 1
          items: { type: string, enum: [openai-chat, openai-responses, anthropic] }
        models:
          type: array
          items: { type: string }
        format_models:
          type: object
          additionalProperties:
            type: array
            items: { type: string }
        model_mapping:
          type: object
          additionalProperties: { type: string }
    AccountPatch:
      type: object
      properties:
        name: { type: string }
        template_id: { type: integer, format: int64 }
        upstream_key: { type: string }
        status: { type: string, enum: [active, unhealthy, "429", disabled] }
        weight: { type: integer }
        max_concurrency: { type: integer }
        # 替换语义：null/缺省 = 不变；[] = 清空；提供列表 = 替换账号全部分组
        group_ids:
          type: array
          items: { type: integer, format: int64 }
    GroupPatch:
      type: object
      properties:
        name: { type: string }
        visibility: { $ref: '#/components/schemas/GroupVisibility' }
    BatchUpdateAccountsBody:
      type: object
      required: [ids, fields]
      properties:
        ids:
          type: array
          items: { type: integer, format: int64 }
          minItems: 1
          maxItems: 100
        fields: { $ref: '#/components/schemas/AccountPatch' }
    BatchUpdateGroupsBody:
      type: object
      required: [ids, fields]
      properties:
        ids:
          type: array
          items: { type: integer, format: int64 }
          minItems: 1
          maxItems: 100
        fields: { $ref: '#/components/schemas/GroupPatch' }
    BatchDeleteResponse:
      type: object
      required: [deleted]
      properties:
        deleted: { type: integer }
    BatchUpdateResponse:
      type: object
      required: [updated]
      properties:
        updated: { type: integer }
    UsageLog:
      type: object
      properties:
        ID: { type: integer, format: int64 }
        RequestID: { type: string }
        GroupID: { type: integer, format: int64 }
        AccountID: { type: integer, format: int64 }
        TemplateID: { type: integer, format: int64 }
        Model: { type: string }
        MappedModel: { type: string, nullable: true }
        Format: { $ref: '#/components/schemas/RequestFormat' }
        StatusCode: { type: integer }
        ErrorType: { $ref: '#/components/schemas/ErrorType' }
        LatencyMS: { type: integer, format: int64 }
        PromptTokens: { type: integer, format: int64 }
        CompletionTokens: { type: integer, format: int64 }
        TotalTokens: { type: integer, format: int64 }
        CacheReadTokens: { type: integer, format: int64 }
        CacheCreationTokens: { type: integer, format: int64 }
        CreatedAt: { type: string, format: date-time }
    LogsResponse:
      type: object
      required: [total, rows]
      properties:
        total: { type: integer, format: int64 }
        rows:
          type: array
          items: { $ref: '#/components/schemas/UsageLog' }
    StatBucket:
      type: object
      properties:
        BucketTime: { type: string, format: date-time }
        GroupID: { type: integer, format: int64 }
        AccountID: { type: integer, format: int64 }
        TemplateID: { type: integer, format: int64 }
        Model: { type: string }
        IsError: { type: boolean }
        RequestCount: { type: integer, format: int64 }
        ErrorCount: { type: integer, format: int64 }
        PromptTokens: { type: integer, format: int64 }
        CompletionTokens: { type: integer, format: int64 }
        TotalTokens: { type: integer, format: int64 }
        CacheReadTokens: { type: integer, format: int64 }
        CacheCreationTokens: { type: integer, format: int64 }
        TotalLatencyMS: { type: integer, format: int64 }
