// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"go-proxy-mini/internal/ent/predicate"
	"go-proxy-mini/internal/ent/redemptioncode"
	"go-proxy-mini/internal/ent/redemptionuse"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// RedemptionUseQuery is the builder for querying RedemptionUse entities.
type RedemptionUseQuery struct {
	config
	ctx        *QueryContext
	order      []redemptionuse.OrderOption
	inters     []Interceptor
	predicates []predicate.RedemptionUse
	withCode   *RedemptionCodeQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RedemptionUseQuery builder.
func (_q *RedemptionUseQuery) Where(ps ...predicate.RedemptionUse) *RedemptionUseQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *RedemptionUseQuery) Limit(limit int) *RedemptionUseQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *RedemptionUseQuery) Offset(offset int) *RedemptionUseQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *RedemptionUseQuery) Unique(unique bool) *RedemptionUseQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *RedemptionUseQuery) Order(o ...redemptionuse.OrderOption) *RedemptionUseQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryCode chains the current query on the "code" edge.
func (_q *RedemptionUseQuery) QueryCode() *RedemptionCodeQuery {
	query := (&RedemptionCodeClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(redemptionuse.Table, redemptionuse.FieldID, selector),
			sqlgraph.To(redemptioncode.Table, redemptioncode.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, redemptionuse.CodeTable, redemptionuse.CodeColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first RedemptionUse entity from the query.
// Returns a *NotFoundError when no RedemptionUse was found.
func (_q *RedemptionUseQuery) First(ctx context.Context) (*RedemptionUse, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{redemptionuse.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *RedemptionUseQuery) FirstX(ctx context.Context) *RedemptionUse {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RedemptionUse ID from the query.
// Returns a *NotFoundError when no RedemptionUse ID was found.
func (_q *RedemptionUseQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{redemptionuse.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *RedemptionUseQuery) FirstIDX(ctx context.Context) int64 {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RedemptionUse entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RedemptionUse entity is found.
// Returns a *NotFoundError when no RedemptionUse entities are found.
func (_q *RedemptionUseQuery) Only(ctx context.Context) (*RedemptionUse, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{redemptionuse.Label}
	default:
		return nil, &NotSingularError{redemptionuse.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *RedemptionUseQuery) OnlyX(ctx context.Context) *RedemptionUse {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RedemptionUse ID in the query.
// Returns a *NotSingularError when more than one RedemptionUse ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *RedemptionUseQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{redemptionuse.Label}
	default:
		err = &NotSingularError{redemptionuse.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *RedemptionUseQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RedemptionUses.
func (_q *RedemptionUseQuery) All(ctx context.Context) ([]*RedemptionUse, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RedemptionUse, *RedemptionUseQuery]()
	return withInterceptors[[]*RedemptionUse](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *RedemptionUseQuery) AllX(ctx context.Context) []*RedemptionUse {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RedemptionUse IDs.
func (_q *RedemptionUseQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(redemptionuse.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *RedemptionUseQuery) IDsX(ctx context.Context) []int64 {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *RedemptionUseQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*RedemptionUseQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *RedemptionUseQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *RedemptionUseQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *RedemptionUseQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RedemptionUseQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *RedemptionUseQuery) Clone() *RedemptionUseQuery {
	if _q == nil {
		return nil
	}
	return &RedemptionUseQuery{
		config:     _q.config,
		ctx:        _q.ctx.Clone(),
		order:      append([]redemptionuse.OrderOption{}, _q.order...),
		inters:     append([]Interceptor{}, _q.inters...),
		predicates: append([]predicate.RedemptionUse{}, _q.predicates...),
		withCode:   _q.withCode.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithCode tells the query-builder to eager-load the nodes that are connected to
// the "code" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RedemptionUseQuery) WithCode(opts ...func(*RedemptionCodeQuery)) *RedemptionUseQuery {
	query := (&RedemptionCodeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCode = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CodeID int64 `json:"code_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RedemptionUse.Query().
//		GroupBy(redemptionuse.FieldCodeID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *RedemptionUseQuery) GroupBy(field string, fields ...string) *RedemptionUseGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RedemptionUseGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = redemptionuse.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CodeID int64 `json:"code_id,omitempty"`
//	}
//
//	client.RedemptionUse.Query().
//		Select(redemptionuse.FieldCodeID).
//		Scan(ctx, &v)
func (_q *RedemptionUseQuery) Select(fields ...string) *RedemptionUseSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &RedemptionUseSelect{RedemptionUseQuery: _q}
	sbuild.label = redemptionuse.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RedemptionUseSelect configured with the given aggregations.
func (_q *RedemptionUseQuery) Aggregate(fns ...AggregateFunc) *RedemptionUseSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *RedemptionUseQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !redemptionuse.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *RedemptionUseQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RedemptionUse, error) {
	var (
		nodes       = []*RedemptionUse{}
		_spec       = _q.querySpec()
		loadedTypes = [1]bool{
			_q.withCode != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RedemptionUse).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RedemptionUse{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withCode; query != nil {
		if err := _q.loadCode(ctx, query, nodes, nil,
			func(n *RedemptionUse, e *RedemptionCode) { n.Edges.Code = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *RedemptionUseQuery) loadCode(ctx context.Context, query *RedemptionCodeQuery, nodes []*RedemptionUse, init func(*RedemptionUse), assign func(*RedemptionUse, *RedemptionCode)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*RedemptionUse)
	for i := range nodes {
		fk := nodes[i].CodeID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(redemptioncode.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "code_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *RedemptionUseQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *RedemptionUseQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(redemptionuse.Table, redemptionuse.Columns, sqlgraph.NewFieldSpec(redemptionuse.FieldID, field.TypeInt64))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, redemptionuse.FieldID)
		for i := range fields {
			if fields[i] != redemptionuse.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withCode != nil {
			_spec.Node.AddColumnOnce(redemptionuse.FieldCodeID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *RedemptionUseQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(redemptionuse.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = redemptionuse.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// RedemptionUseGroupBy is the group-by builder for RedemptionUse entities.
type RedemptionUseGroupBy struct {
	selector
	build *RedemptionUseQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *RedemptionUseGroupBy) Aggregate(fns ...AggregateFunc) *RedemptionUseGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *RedemptionUseGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RedemptionUseQuery, *RedemptionUseGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *RedemptionUseGroupBy) sqlScan(ctx context.Context, root *RedemptionUseQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RedemptionUseSelect is the builder for selecting fields of RedemptionUse entities.
type RedemptionUseSelect struct {
	*RedemptionUseQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *RedemptionUseSelect) Aggregate(fns ...AggregateFunc) *RedemptionUseSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *RedemptionUseSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RedemptionUseQuery, *RedemptionUseSelect](ctx, _s.RedemptionUseQuery, _s, _s.inters, v)
}

func (_s *RedemptionUseSelect) sqlScan(ctx context.Context, root *RedemptionUseQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
